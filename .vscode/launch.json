{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Day",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/zig-out/bin/${fileBasenameNoExtension}",
            "preLaunchTask": "build",
            "MIMode": "lldb",
            "cwd": "${cwd}"
        }
    ]
}